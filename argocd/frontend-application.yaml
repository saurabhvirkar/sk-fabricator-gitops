apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: skfabricator-frontend-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/saurabhvirkar/sk-gitops-manifests.git'
    targetRevision: HEAD
    path: helm/frontend
    helm:
      valueFiles:
        - '../../environments/dev/frontend-values.yaml'
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
