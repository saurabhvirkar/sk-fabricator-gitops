replicaCount: 2

image:
  repository: saurabhvirkar/skfabricator-frontend
  tag: "stage-latest"

ingress:
  enabled: true
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
  hosts:
    - host: stage.skfabricator.com
      paths:
        - path: /
          pathType: Prefix

configMap:
  data:
    API_URL: "https://stage-api.skfabricator.com"
    ENVIRONMENT: "stage"
