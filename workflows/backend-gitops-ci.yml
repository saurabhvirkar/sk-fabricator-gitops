# Place this file in SkFabricatorAndErector-Backend/.github/workflows/gitops-ci.yml when enabling GitOps CI
name: GitOps Backend CI (Docker Build & Registry Push)

on:
  push:
    branches: [ develop, main ]
    paths:
      - 'src/**'
      - 'Dockerfile'
      - 'SkFabricatorAndErector.slnx'
  pull_request:
    branches: [ develop, main ]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/skfabricator-backend

jobs:
  build-test-push:
    name: Build, Test, Security & Docker Push
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup .NET SDK 10.0
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore Dependencies
        run: dotnet restore SkFabricatorAndErector.slnx

      - name: Build Solution
        run: dotnet build SkFabricatorAndErector.slnx --no-restore -c Release

      - name: Execute Unit & Integration Tests
        run: dotnet test SkFabricatorAndErector.slnx --no-build -c Release --verbosity normal

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry (GHCR)
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=,format=short
            type=ref,event=branch
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build & Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
